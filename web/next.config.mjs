/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // PWA support will be added in Step 2 via next-pwa plugin
  // For now this is a basic Next.js 14 config
};

export default nextConfig;
